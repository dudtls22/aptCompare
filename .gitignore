# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.idea/
.vscode/

# env / secrets (서비스키 등은 여기 두고 코드에서 읽도록 분리 권장)
.env
.env.*

# 로컬 즐겨찾기 폴백 (Upstash 미설정 시)
.data/

# Vercel 빌드 시 public/ 복사본 (루트 index.html·api/ 와 혼동 방지)
public/*
!public/.gitkeep
